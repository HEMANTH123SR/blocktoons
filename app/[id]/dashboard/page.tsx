import { WebToonSubNavigator } from "@/components/WebToonNavigator";
const Dash = ({ children }: { children: React.ReactNode }) => {
  return <WebToonSubNavigator>{children}</WebToonSubNavigator>;
};

export default Dash;